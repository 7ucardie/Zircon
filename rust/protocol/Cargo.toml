[package]
name = "zircon-protocol"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Zircon wire-protocol codec (client ↔ server packets)"

[dependencies]
bytes     = { workspace = true }
thiserror = { workspace = true }

[dev-dependencies]
criterion = { workspace = true }

[[bench]]
name    = "framing"
harness = false
